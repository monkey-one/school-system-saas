<section class="bg-gradient-to-r from-navy-800 to-navy-600 text-white">
    <div class="mx-auto max-w-7xl px-4 py-12 sm:px-6">
        <h1 class="font-heading text-3xl font-extrabold sm:text-4xl">{{ $title }}</h1>
        @if (! empty($subtitle))<p class="mt-2 text-white/75">{{ $subtitle }}</p>@endif
    </div>
</section>
